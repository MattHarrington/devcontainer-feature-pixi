{
    "name": "pixi",
    "id": "pixi",
    "version": "1.3.0",
    "description": "Installs the pixi package manager (https://pixi.sh) as a system-wide binary.",
    "documentationURL": "https://pixi.sh",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest"
            ],
            "default": "latest",
            "description": "Version of pixi to install. Use 'latest' or a semver such as '0.68.0' (a leading 'v' is optional)."
        },
        "bioconda": {
            "type": "boolean",
            "default": false,
            "description": "Configure the Bioconda channel by writing a system-wide pixi config at /etc/pixi/config.toml with default-channels = [\"conda-forge\", \"bioconda\"]."
        }
    },
    "keywords": [
        "pixi",
        "package-manager",
        "conda",
        "bioconda",
        "bioinformatics",
        "python"
    ],
    "mounts": [
        {
            "source": "pixi-${devcontainerId}",
            "target": "${containerWorkspaceFolder}/.pixi",
            "type": "volume"
        }
    ],
    "postCreateCommand": "/usr/local/share/pixi/post-create.sh \"${containerWorkspaceFolder}\"",
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils"
    ]
}
